# Agent roles

These are optional role definitions for hosts that support specialist agents.
Operations Pulse starts with one operations lead; specialists are selected only
when a bounded task needs them. Delegation never expands permissions.

Each role definition uses the same portable contract:

- mission;
- use cases;
- owned evidence and paths;
- tools;
- guardrails;
- required output.
